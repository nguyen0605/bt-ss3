#include <iostream>
#include <stdio.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) {
	float celsius, fahrenheit;
	printf("Hay nhap nhiet do theo do celsius: ");
	scanf("%f", &celsius);
	printf("Nhiet do sau khi doi tu celsius sang fahrenheit la: %.2f do", fahrenheit=celsius*1.8+32.0);
	return 0;
}
